/**
 * A simple Webpack loader that adds a data-component attribute to React components for visual editing.
 * This is a placeholder and does not actually parse or modify code.
 * For real use, you'd use Babel or SWC for AST-based code transforms.
 */
module.exports = function(source) {
  // This is a stub. In real scenarios, you'd parse the source and inject attributes.
  // For now, just return the source unchanged.
  return source;
};